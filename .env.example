# YMCA Auto-Signup Configuration
# All variables are OPTIONAL - the application is now fully configurable via the Settings UI

# Environment (defaults to 'production')
NODE_ENV=development

# Server Port (defaults to 3001)
PORT=3001

# Session Secret (OPTIONAL - auto-generated and stored in database if not provided)
# The app automatically generates and persists a session secret in the database on first run
# Only set this if you want to override the database-stored secret
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# SESSION_SECRET=your-secret-key-change-this-in-production

# YMCA Credentials (can be set in Settings UI instead)
# If not provided here, configure via Settings tab in the web interface
YMCA_EMAIL=your-email@example.com
YMCA_PASSWORD=your-password

# YMCA API URLs (system defaults - only override if using a different YMCA system)
YMCA_URL=https://ymca-triangle.fisikal.com
API_BASE_URL=https://ymca-triangle.fisikal.com/api/web
